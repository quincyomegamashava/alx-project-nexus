import RegisterForm from '../../components/auth/RegisterForm';

export default function RegisterPage() {
  return <RegisterForm />;
}

export const metadata = {
  title: 'Register - Project Nexus',
  description: 'Create a new Project Nexus account',
};